# Archived Unused Configs And Scripts

現在の運用で使っているのは、主に次の 3 つです。

- `track_config_2.json`
- `track_config_2_bt_cuda_lite_dual_eval.json`
- `track_config_2_capture_images.json`

このフォルダには、現行のチェッカーボード撮影と座標統合評価フローでは使っていない補助ファイルを退避しています。

- 旧 body tracking 診断用 config
- `capture_only` 切り分け用 config
- VRAM 計測用 PowerShell スクリプト

削除ではなく退避なので、必要になったら root へ戻すか、直接このフォルダのパスを指定して使えます。
